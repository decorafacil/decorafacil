<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.Decorafacil" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Adicione a cor da Barra de Status -->
        <item name="android:statusBarColor">@color/blueDefault</item>
    </style>

    <!-- Base application theme for dark theme -->
    <style name="Base.Theme.Decorafacil.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <!-- Adicione a cor da Barra de Status para o tema escuro -->
        <item name="android:statusBarColor">@color/blueDefault</item>
    </style>

    <style name="Theme.Decorafacil" parent="Base.Theme.Decorafacil" />

</resources>
